# btop.conf — bản tối giản, chỉ set những gì cần đổi so với mặc định.
# btop tự điền đầy đủ các key còn thiếu + ghi lại file này khi chạy lần đầu,
# không cần liệt kê hết mọi setting ở đây.

color_theme = "catppuccin_mocha"
theme_background = False
vim_keys = True
update_ms = 1500
